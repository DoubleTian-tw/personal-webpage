export interface classificationProps {
    classification: string;
}
export interface portfolioProps {
    id: number;
    classification: string;
    name: string;
    src: string;
    link: string;
    description: string;
}
